# Chapter 2 V3 — paragraph outline (regenerated 2026-07-14)

One line per paragraph, themed and tagged. Tags as in the Ch 1 outline.

## 2.1 The matrix is a verb

1. Opener: the Method's tour, owed to this chapter's object. {method}
2. Data lens: a matrix is a dataset; Def 2.1 (data-matrix conventions). {data}{def}
3. Two readings; rows are points; Fig 2.1 (houses TikZ, re-shown). {method}{fig}
4. Algebraic beat (array with entries) + computational beat; Listing 2.1 (shape). {method}{code}
5. Words become vectors later; container is the smaller half; the verb thesis. {blow}
6. Def 2.2 (linear transformation); pass align (doubling) and fail align (squaring); shift fails quietly. {def}{math}
7. Claim 2.3 (matrices ARE the linear transformations) boxed with the rotation witness + landing footnote; Fig 2.2 (landing-spots TikZ); not storing its columns. {claim}{fig}{blow}

### The verb that differentiates

8. The D exhibit; the display; symbolic difference-quotient align; row by row it IS differentiation. {math}{blow}
9. Listing 2.2 (built) + Listing 2.3 (tested) + Listing 2.4 (drawn) + Fig 2.3. {code}{fig}
10. K, the second difference matrix; Listing 2.5; provenance footnote. {code}

## 2.2 One product, two views

11. Def 2.4 (both views); row align; column align; same sixteen multiplications. {def}{math}
12. Claim 2.5 (views agree) boxed; Listing 2.6 (defined) + Listing 2.7 (run). {claim}{code}
13. Row = compute, column = understand; memory-address footnote. {blow}

### Composition

14. Def 2.6 (matrix-matrix product); Claim 2.7 boxed + double-sum footnote. {def}{claim}
15. Order matters; the high-school payoff; Fig 2.4 (rotation TikZ); Claim 2.8; Listing 2.8 + Listing 2.9; trig footnote; machine-knew verdict. {fig}{claim}{code}

### The identity, the transpose, and the undo

16. Defs 2.9–2.11 (identity, transpose, inverse). {def}
17. Differencing undone by summing: hand align (telescoping); Claim 2.12 boxed; Listing 2.10; fundamental-theorem cameo. {math}{claim}{code}{blow}

## 2.3 Rotate, scale, project

18. Watch the verb: Listing 2.11 (defined) + Listing 2.12 (watched) + Fig 2.5; rotation/scaling verdicts; the third panel. {code}{fig}
19. Projection by the creed: shadow prose, pencil aligns, Fig 2.6 (shadow TikZ), THEN Def 2.13 (formula last); Claim 2.14 boxed + algebra footnote; Listing 2.13 + Listing 2.14 + Fig 2.7; look-longer paragraph (the preface's destination). {method}{math}{fig}{def}{claim}{code}{blow}

## 2.4 Running the verb backwards

20. Forward vs backward; the standing pair; Existence; Def 2.15 (column space); the Chapter 1 tie (span unnamed until here). {method}{def}{data}
21. Uniqueness as the license. {method}
22. The membership system, re-run as a matrix equation. {math}
23. The difference matrix behaves; the cyclic matrix collapses; Listing 2.15. {code}
24. Def 2.16 (null space); Claim 2.17 (subspace) boxed; Claim 2.18 (invertibility) boxed + accounting footnote. {def}{claim}
25. The geometry in one look; overdetermined and underdetermined regimes = the two estimation chapters. {blow}

## 2.5 Standardization is a transformation

26. The units problem in Chapter 1's weights. {data}
27. Standardization displayed; what a step of one means. {math}
28. Honesty box: affine, not linear; disclosed. {method}
29. One-hot encoding; two planted consequences (group means; the dummy trap in null-space vocabulary). {data}
30. Gelman's two standard deviations + footnote; the two conventions leave side by side. {data}

## 2.6 A covariance-ready Ames, twice

31. Listing 2.16 (standardize + prove it) + Listing 2.17 (drawn) + Fig 2.8. {code}{fig}
32. Listing 2.18 (shared units); the story inverts; the cheapest important lesson. {code}{data}{blow}
33. Listing 2.19 (Gelman matrix) + Listing 2.20 (one currency); verdicts. {code}{data}
34. Exit state: Z for Chapter 6, X_g for Chapter 11. {blow}

## 2.7 Summary and exercises

35. Summary; Chapter 3 tease (eigenvectors inside K); 12 exercises. {blow}
