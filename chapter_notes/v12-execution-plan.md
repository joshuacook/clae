# v12 execution plan (from the v11 ink census, 2026-07-23)

Source of truth: chapter_notes/v11-ink-census-2026-07-23.md. Work on
branch v9-tranche-2-3, drafts in chapter_notes/drafts-v9/. Ink on v11
produces v12.

## Order of work

1. **Render fix (DONE):** claim env off the shared counter; drop
   `envcountsame` from every main tex at restage. Verify per-type
   numbers in the next PDF match source.
2. **Preface re-centered on TRANSFER (P8, the big one).** New spine:
   epiphany = transfer + estimation. The night-class acts reframed so
   each idea is an instance of TRANSFER (basis/waves, orthogonality,
   estimation); add the log-transform / Poisson beat in Josh's register;
   transformations approximate functions; polynomials are linear. Career
   arc CUT to what serves transfer or estimation, with plots not résumé
   (P11). Lockhart moves up near "Jim is a mystic" (P7). Two-roads
   rewrite: old road = the calc-2/diffeq computation pipeline with LA as
   a stop on it (P2/P5/P6, semester not year). Lens christening rewritten
   so creed → four lenses connects explicitly (P4). "You took linear
   algebra" close hits the wrong-path point hard (P13). Fig 0.3 replaced:
   cos(x) at h=0.25/0.1/0.01, blocky → smooth (P9). Fig 0.4 lands via
   transfer or moves (P10).
3. **Existence/uniqueness + solving move to the preface review (P14/C12/
   C18):** given to Jim (his idea), with the membership question actually
   SOLVED there (by inspection + TikZ, C7 satisfied at its new home).
   Review also reconciles dedup: D-act vs ch2 build (C21) and the
   product entry-rule vs ch2 (C22) — preface shows, chapters own.
4. **Ch1 re-centered on THE AGREEMENT (C1, C3):** intro states the one
   idea; vector space defined FIRST (or the agreement stated before the
   vector), "if we agree that a vector space is what these two
   operations define"; R^2 = "point" callback + coordinates early (C2/
   C16); standard basis = the x and y axes (C14); subspace demoted
   (Axler-tangent trimmed, C9) — span carries; independence test shows
   all-weights-zero = trivial solution with the flawed R^2/R^3 figure
   corrected (C10: three vectors, two independent, in R^3); rank/column
   space fold into the tour (C13 placement); "span of the question"
   name killed (C15); same-space clarified at eq 1.5 (C4); LLM footnote
   "architecture died" line rewritten (C5); span_cloud/sweeper naming
   unified on "swept" (C6); solving question OUT (to preface review),
   1.6 axpy + 1.7 function stay (C19 likes).
5. **Claims → re-thought (C11, book-wide):** the word "claim" retired
   (colloquial tic); each becomes either a named Fact box with a real
   digression (usable fundamental fact) or inline prose. Decide format
   with Josh if ambiguous; default: "Fact" env, longer treatment.
6. **Ch2 opens FROM the agreement (C20):** operate on a vector, stay in
   the space, Chapter 1 applies; dedup D + product vs preface (C21/C22)
   per the review reconciliation in step 3.
7. Renumber (per-type, dense), paracheck, G1, render v12 draft book,
   deliver.

## Open questions surfaced to Josh (2026-07-23 reply)

- Claim replacement format (Fact-with-digression vs inline prose).
- Subspace: demote-but-keep (recommended) vs remove entirely.
- Vector space before vector (his "probably" — recommended: yes, as the
  agreement-first opening).
