# v11 ink census (2026-07-23, delivered in-session, no highlighting)

Josh read preface + ch1 + into ch2 ("not a full read... but substantial,
enough to do a new version"). Verbatim capture, numbered for the punch
list. Ink on v11 produces v12.

## Preface

- **P1** "Dashed by a windmill named after Gilbert Strang" (opening line note, as written).
- **P2** "The old road runs on determinants" → the old road runs completely
  through calc 2 / diff eq, and linear algebra is frequently treated as
  just another stop on that road, which is why everything is computation.
- **P3** "Let geometry and computation carry the argument" needs emphasis.
- **P4** The four-lenses christening is NOT well connected: creed lists
  three moves, then suddenly four lens names appear; "It's just not good
  writing." Likes "start from the columns...", likes "pictures before
  procedures, actions before formulas", but the connection to the book's
  treatment is missing. ("I do like these four lenses. I just don't
  think we introduced them correctly.")
- **P5** "Geometry is the outlier of high school mathematics" — connect
  this to the earlier determinant-road path.
- **P6** "Linear algebra is where the pipeline breaks open for good" —
  but there are still two paths; for many it never breaks open though it
  could have if taught right. Also: it's a semester course, not a year.
- **P7** Move Lockhart EARLIER, near "Jim is a mystic" — could be a great
  transition out of the quixotic scene.
- **P8 (BIG, the preface's new center):** "One idea from that night class
  kept transferring" — the emphasis must be on THE TRANSFER, not the
  idea. Ideas from linear algebra transfer again and again (his live
  example: customer-archetype PCA → genetics, same math). "Transfer is
  the point... once you assume linearity it's all linear algebra, and if
  you can't assume linearity, take the log... nine times out of ten...
  and if you can't, it's probably a Poisson problem." Focus on transfer
  and the whole rest of the preface becomes the fantastic ideas that
  transfer: basis sets and waves, orthogonality, ultimately estimation.
  Plus: transformations are computational approximations for functions;
  polynomial functions are linear.
- **P9** Figure 0.3 (error vs grid loglog): still hated. The way to show
  it: plot cos(x), the derivative, at different granularities — blocky
  at 0.25 steps, taking shape at 0.1, smooth to the eye at 0.01.
- **P10** Figure 0.4 (standing waves) comes out of nowhere; pushing
  TRANSFER through the rest of the preface may fix its landing.
- **P11** The career arc after school is a wall of text. "No one cares
  about my résumé. The point is the math and we need plots and we need
  transfer. Transfer and estimation — if it doesn't serve either of
  those two ideas it doesn't belong here."
- **P12** The review: good progress, still not sold.
- **P13** "You took linear algebra, maybe more than once" — HIT IT HARD
  here: you were probably marched down the wrong path and never got to
  see the correct one.
- **P14 (STRUCT)** The solving question / existence-uniqueness could move
  to the PREFACE, in the review section, given to a discussion about Jim
  ("it's his idea") — and actually SOLVE it there. Then rank and column
  space become part of Chapter 1's tour. (Pairs with C11.)

## Chapter 1 — central idea: THE AGREEMENT

- **C1 (BIG):** the chapter-1 intro is a list, needs a CENTRAL IDEA: the
  agreement. "Everything in the chapter needs to be what we are agreeing
  to and what we're agreeing about." (Preface's central idea = epiphany:
  transfer and estimation; Ch1's = the agreement.)
- **C2** At R^2: say "you've probably seen this before under the name
  'point'." (Also: coordinates might belong earlier, at the point
  remark — see C16.)
- **C3** "The vector supports exactly two operations" is wrong framing —
  what we mean is: IF we agree that a vector space is the thing defined
  by these two operations. "Would it be crazy to define vector space
  before we define vector, probably..."
- **C4** Equation 1.5 region: clarify all vectors are from the SAME
  vector space.
- **C5** Footnote 3: "why do you say the architecture died…" (LLM/
  attention footnote).
- **C6** Listing 1.10: title says "span sweeper," function is span_cloud
  — pick one; probably "swept" since used elsewhere.
- **C7** "Membership in a span is a concrete question" — great question,
  NOT ANSWERED there. Answer it on the spot: by inspection, 1v + 2w,
  and draw the geometric view in TikZ.
- **C8 (RENDER BUG):** "I thought we had agreed to separate numbering
  for definitions and claims" — sees Definition 1.5 then Claim 1.6.
  ALSO: prose refs (e.g. "Claim 1.2" under the license) point at claim
  numbers that don't exist in the render. [Root cause found: the SNmono
  class option envcountsame merges all theorem counters at render time,
  overriding the per-type source numbering.]
- **C9** Definition/claim for span-is-subspace is "a very Axler proof"
  — reads as tangent; "too much Axler derails us." Span is the
  important idea right now; SUBSPACE may move later or go entirely.
  (But: likes the word subspace inside the basis definition.)
- **C10** The math at the independence test (old Claim-1.7 region):
  still disliked; SHOW all weights forced to zero and call that the
  trivial solution. AND the third-vector figure is flawed: in R^2 you
  cannot have a vector out of the plane / three independent vectors —
  that drawing only makes sense in R^3 with three vectors of which two
  are independent. "I need you to be on top of stuff like this."
- **C11** The claims are falling flat; unsure what they're gaining.
  ALSO **the word "claim" itself reads as the colloquial-language tic**
  — each one deserves a longer digression leaving the reader a usable
  fundamental fact, not a boxed one-breath. (Applies book-wide.)
- **C12** By-inspection + existence/uniqueness treatment: still not
  happy. (Feeds P14.)
- **C13** The COORDINATES definition ("definition 1.10" in the garbled
  render numbering; Josh clarified he means coordinates): "doesn't add
  anything I guess" where it sits — rather see it earlier, at the
  "point" remark (merges with C16).
- **C14** Standard basis: HUGE missed opportunity — it's the x and y
  axes.
- **C15** "WTF is a span of the question" (the Claim named 'span of the
  question' — rename/kill).
- **C16** Coordinates definition might come earlier, at the "point"
  remark (C2).
- **C17** Likes equation 1.10's introduction as-is.
- **C18** The solving question is "almost a different conversation from
  the agreement" → see P14: existence/uniqueness + solving to the
  preface review (Jim's idea, actually solved there); rank + column
  space fold into Ch1's tour.
- **C19** 1.6 (axpy) "gets back to the agreement — it's great."

## Chapter 2

- **C20** Operator direction is right but must come DIRECTLY from the
  agreement: you operate on a vector, it's still in a vector space, so
  everything in Chapter 1 continues to apply.
- **C21** The difference matrix is covered in the preface AND again in
  ch2 — dedup (the preface D-act needs to slim or the ch2 build must
  not repeat it).
- **C22** Matrix multiplication was done in the preface, why reviewed
  again in ch2 (the entry-rule remainder needs reconciling with the
  dedup rule).

## Notes on process

- Version: this ink produces **v12**.
- C8 root cause is in the render class options, not the source; fix in
  tools/render (drop envcountsame or give each env its own counter).
